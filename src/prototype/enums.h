#ifndef ENUMS_H
#define ENUMS_H

enum struct IndentationMode
{
    Spaces,
    Tabs
};

enum struct SwitchCommand
{
    NextModel,
    NextPanel,
    PreviousModel,
    PreviousPanel
};

enum struct Direction
{
    Left,
    Up,
    Right,
    Down
};


#endif // ENUMS_H

